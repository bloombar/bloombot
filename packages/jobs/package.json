{
  "name": "@bloombot/jobs",
  "version": "0.1.0",
  "private": true,
  "description": "The background job queue's runner (JOB-1..3): a handler registry, one atomic claim, and the retry-with-backoff policy — plus JOB-4's admission gate, bounding concurrent model calls in packages/core's answering path.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@bloombot/db": "*",
    "@bloombot/logger": "*"
  }
}
