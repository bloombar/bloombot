{
  "name": "@bloombot/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "The MCP server (MCP-1..5): the third apps/ process, exposing packages/actions' own catalog to an AI assistant as an explicit, allow-listed set of tools — the same dispatcher, the same policies, the same refusals apps/api uses, never a parallel implementation.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@bloombot/actions": "*",
    "@bloombot/auth": "*",
    "@bloombot/config": "*",
    "@bloombot/db": "*",
    "@bloombot/logger": "*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.2.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4"
  }
}
