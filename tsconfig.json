{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Solution file. It compiles nothing itself; `tsc --build` walks these references in dependency order (PLAT-1).",
  "files": [],
  "references": [
    { "path": "./packages/config" },
    { "path": "./packages/logger" },
    { "path": "./packages/schemas" },
    { "path": "./packages/db" },
    { "path": "./tsconfig.tests.json" }
  ]
}
